export type ContentSidebarWidth = "narrow" | "medium" | "wide";

export const SIDEBAR_WIDTH_CLASSES: Record<ContentSidebarWidth, string> = {
  narrow: "w-[200px]",
  medium: "w-[256px]",
  wide: "w-[320px]",
};
