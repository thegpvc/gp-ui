/**
 * Common icon re-exports from lucide-react
 *
 * Icons used in design system components:
 * - Loader2 (Button loading state)
 * - TrendingUp, TrendingDown (StatCard trends)
 * - Info, AlertTriangle, X, CheckCircle, XCircle (Alert variants)
 *
 * Common app-level icons for convenience.
 */

// Icons used in design system components
export {
  Loader2,
  TrendingUp,
  TrendingDown,
  Info,
  AlertTriangle,
  X,
  CheckCircle,
  XCircle,
} from 'lucide-react'

// Common app-level icons
export {
  Search,
  User,
  Mail,
  Calendar,
  ArrowLeft,
  ArrowRight,
  Menu,
  LogOut,
  Terminal,
  RefreshCw,
  ExternalLink,
} from 'lucide-react'

// Icon type for TypeScript
export type { LucideIcon } from 'lucide-react'
