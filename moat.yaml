name: gp-ui
dependencies:
  - node@22
  - git
  - gh
grants:
  - github
hooks:
  pre_run: npm install
mcp:
  - name: figma
    url: https://mcp.figma.com/mcp
    auth:
      grant: mcp-figma
      header: Authorization
